# Dependencias y salidas de build de la app del gimnasio.
#
# El bundle no se construye aca: vite.config.ts lo escribe en
# ../target/classes/static/gym-app para que el jar de Spring lo sirva, y esa
# carpeta ya la ignora el .gitignore de la raiz.
node_modules/
dist/
tsconfig.tsbuildinfo
